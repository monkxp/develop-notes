useQueryStates is a hook that allows you to manage state in the url.

## Usage

```tsx
import useQueryStates from "./useQueryStates";

const [selectedColor, setSelectedColor] = useQueryStates("color");
```
